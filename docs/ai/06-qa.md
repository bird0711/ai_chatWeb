# QA Log

## 2026-05-02 v1.0 File Upload Slice

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。
  - 覆盖：AI prompt 文件上下文、OpenAI-compatible 请求体、服务层文件传递、HTTP 页面上传入口、`.docx` 文本提取、基础 PDF 文本提取、既有主路径回归。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 尚未在本环境完成浏览器上传文件并调用真实模型 API 的端到端人工验证。
- 原因：本环境之前缺少 Chrome/Chromium/Firefox，且 MySQL/Redis 本地 TCP 访问在 sandbox 中可能受限；本切片先以自动化测试和构建作为代码级证据。

### 建议人工验收路径

1. 按 `README.md` 启动项目。
2. 登录并进入已有或新建群聊。
3. 在“文件资料”面板上传 `.txt`、`.md`、`.docx` 或可复制文本型 `.pdf` 文件。
4. 确认文件出现在文件列表。
5. 发送“请基于刚才上传的文件总结要点”。
6. 确认 AI 回复引用或分析了文件内容。
7. 确认可以从当前电脑直接选择本地 `.docx` 和 `.pdf` 文件上传。
8. 确认可以把本地文件拖到上传区域后提交。

## 2026-05-02 v1.0 Controlled Tools Slice

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。
  - 覆盖：服务层工具成功/失败记录、系统消息生成、HTTP 工具入口、工具执行路由和结果显示。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 建议人工验收路径

1. 按 `README.md` 启动项目。
2. 登录并进入任意群聊。
3. 打开“受控工具”面板。
4. 选择“四则计算”，输入 `12 * (3 + 4)`。
5. 点击“运行工具”。
6. 确认消息流出现系统消息，结果为 `84`。
7. 确认工具执行记录显示 `calculator · success`。
8. 输入 `1 / 0` 再执行，确认失败状态和错误信息可见。

## 2026-05-03 Model API Timeout Retry Verification

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。
  - 覆盖：模型 API 瞬时 TLS timeout 重试成功、重复 TLS timeout 返回中文错误、既有服务层和 HTTP 主路径回归。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 本地服务验证

- 沙箱内直接启动失败，原因是本地 MySQL TCP socket 被限制。
- 使用批准的提升权限启动后，服务成功连通 MySQL 和 Redis，并监听 `http://localhost:8080`。
- 通过 HTTP 验证：
  - `/login` 可访问。
  - 临时账号可以注册。
  - 临时账号可以访问群聊列表。
  - 临时账号可以创建并删除临时群聊。
  - 临时账号可以退出登录。

### 当前未完成的真实交互验证

- 未完成用户本机浏览器中的真实 AI 回复验证。
- 原因：当前环境没有 Chrome/Chromium/Firefox；临时账号没有用户保存的模型 API 配置，不能验证真实 `/chat/completions` 供应商路径。

### 建议人工验收路径

1. 按 `README.md` 启动项目。
2. 使用已配置模型 API 的账号登录。
3. 进入至少有两个可发言 AI 角色的群聊。
4. 发送一条普通消息。
5. 确认不会立即出现原始 `net/http: TLS handshake timeout`。
6. 如果模型供应商可达，确认至少两个 AI 角色能完成回复。
7. 如果供应商仍然超时，确认系统消息显示中文超时说明，而不是原始 Go transport 错误。

## 2026-05-03 AI Review Async Toggle Bug Fix

### 复现条件

- 在群聊详情页点击“开启互评”或“关闭互评”。
- 修复前表单提交到 `/chats/:chatID/ai-review` 后会收到 `302`，导致整页刷新。

### 预期行为

- 支持 JavaScript 的浏览器中，AI 互评开关应异步切换并局部更新状态。
- 无 JavaScript 或非 JSON 请求仍可通过普通表单提交和重定向完成切换。

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：AI 互评异步表单标记、普通表单重定向、JSON 请求返回当前开关状态。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 本地服务验证

- 服务以提升权限启动在 `http://localhost:8081`，成功连通 MySQL 和 Redis。
- 通过 HTTP 验证：
  - 临时账号注册成功。
  - 临时群聊创建成功。
  - `POST /chats/6/ai-review` 携带 `Accept: application/json` 返回 `200 OK` 和 `ai_review_enabled: true`，没有返回 `302`。
  - 受控工具计算器路径可执行。
  - 临时群聊删除和退出登录成功。

### 当前未完成的真实交互验证

- 当前环境没有 Chrome/Chromium/Firefox，不能执行真实点击级浏览器验证。
- 建议用户在本机浏览器确认点击“开启互评/关闭互评”时页面不刷新，状态文字原地变化。

## 2026-05-03 v1.0 Multi-Provider/Multi-Model Routing Slice

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/app`
  - 结果：通过。
  - 覆盖：普通回复按角色绑定的模型配置调用；AI 互评按角色绑定的模型配置调用。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：设置页路由编号/供应商/Base URL 展示；聊天页角色路由元数据展示；既有角色编辑和多 API 配置路径回归。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 当前环境没有浏览器，未完成真实点击级检查。
- 建议用户在本机浏览器检查模型设置页和角色卡片中的路由信息，并发送消息确认不同角色仍正常回复。

### 建议人工验收路径

1. 在模型设置中保存两个 API 配置。
2. 进入群聊，为两个 AI 角色分别选择不同的 API 配置和模型。
3. 确认设置页能看到“路由 #ID · provider · 默认模型 · Base URL”。
4. 确认角色卡片能看到“路由 #ID · 配置名称 · provider · 模型”。
5. 发送普通消息，确认两个角色仍正常回复。
6. 开启 AI 互评，确认互评回复仍正常追加。

## 2026-05-03 v1.0 Deployment Documentation Slice

### 文档验证

- `docs/ai/deployment.md` 已新增。
- `README.md` 已链接 `docs/ai/deployment.md`。
- `docs/ai/developer-settings.md` 已链接 `docs/ai/deployment.md`。
- 对照当前代码和 `.env.example` 检查了部署文档中的关键变量和路径：
  - `ADDR`
  - `MYSQL_DSN`
  - `REDIS_ADDR`
  - `REDIS_PASSWORD`
  - `REDIS_DB`
  - `TEMPLATE_GLOB`
  - `STATIC_DIR`
  - `UPLOAD_DIR`
  - `CHAT_FILE_DIR`
  - model API timeout/retry variables
  - `/health`
  - `/uploads`

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 未实际部署到生产主机。
- 未验证真实域名、TLS 证书、反向代理或 systemd 单元。
- 这些内容按本切片边界只提供文档，不执行生产部署。

### 建议人工验收路径

1. 阅读 `README.md` 中的部署文档链接。
2. 打开 `docs/ai/deployment.md`。
3. 确认构建、环境变量、持久化目录、反向代理、健康检查、备份和回滚说明能覆盖你的目标部署方式。
4. 如果你的目标环境不是单机/VM 自托管，把差异记录为后续部署增强需求。

## 2026-05-03 v1.0 Observability/Logging/Error Strategy Slice

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：聊天 action error 会输出 `chat_action_error`、action、chat ID 和 HTTP status。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 文档验证

- `docs/ai/observability.md` 已新增。
- 文档覆盖当前 baseline、日志策略、错误报告策略、操作检查和未来增强。
- 文档明确不记录 API Key、session token、上传文件全文、完整聊天历史和密码哈希。

### 当前未完成的真实交互验证

- 未接入外部日志平台或错误报告服务。
- 未实现 metrics、alerting、tracing 或 request ID。

### 建议人工验收路径

1. 启动服务并观察 stdout/stderr。
2. 触发一个聊天页校验错误，例如在少于两个可发言角色时异步发送消息。
3. 确认日志出现 `chat_action_error`。
4. 模型供应商不可用时发送消息，确认聊天系统消息提示失败，同时服务日志出现 `async_ai_reply_error`。

## 2026-05-03 v1.0 CI Checks Slice

### 自动化验证

- `sh scripts/ci-check.sh`
  - 结果：通过。
  - 覆盖：`go test -mod=mod ./...` 和 `go build -mod=mod -buildvcs=false ./cmd/server`。

### 文档验证

- `.github/workflows/ci.yml` 已新增。
- `docs/ai/ci.md` 已新增。
- `README.md` 已记录 `sh scripts/ci-check.sh`。

### 当前未完成的真实交互验证

- 未在 GitHub Actions 远端 runner 上执行，因为当前环境不推送代码。
- 工作流使用 `go-version-file: go.mod`；如果远端 runner 暂不支持 `go.mod` 中的 Go 版本，需要等待 runner 支持或调整版本策略。

## 2026-05-03 AI Review Natural-Reply Prompt Optimization

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/ai`
  - 结果：通过。
  - 覆盖：互评 system prompt 包含自然接话、选择一个观点、互动方式、短段落、避免报告式表达和不要重复第一轮回答等约束；互评 conversation prompt 要求用群聊自然语气接话。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 未调用真实模型 API 判断输出口吻，因为当前环境不读取用户保存的模型 API Key，也不能替代用户浏览器中的真实体验。

### 建议人工验收路径

1. 使用已配置真实模型 API 的账号登录。
2. 打开一个至少有两个可发言 AI 角色的群聊。
3. 开启 AI 互评。
4. 发送一个适合讨论的问题。
5. 检查互评追加回复是否像接住某个角色观点继续聊，而不是正式报告。
6. 检查互评回复是否自然提到或回应某个角色，且没有大段“首先/其次/综上”式总结。
7. 检查互评回复没有明显复读自己的第一轮回答。

## 2026-05-03 Selective AI Participation Optimization

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/app`
  - 结果：通过。
  - 覆盖：三个角色时首轮选择两个回复；短消息跳过互评；长消息最多追加一个互评；AI review 仍按角色绑定的模型配置路由。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：AI 互评开关、异步消息追加、短消息首轮回复、长消息互评追加。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 未调用真实模型 API 判断实际群聊观感；当前环境不能替代用户保存模型配置下的浏览器体验。

### 建议人工验收路径

1. 准备一个有三个或更多可发言 AI 角色的群聊。
2. 开启 AI 互评。
3. 发送一条很短的问题，例如“继续”或“怎么看？”。
4. 确认不是所有 AI 角色都回答，并且短消息没有固定追加互评。
5. 发送一条更适合讨论的问题，例如“这个方案的主要风险是什么，应该先做哪个步骤？”
6. 确认首轮不是所有角色都回答。
7. 确认互评最多追加一个自然跟进，而不是每次两个固定互评。

## 2026-05-03 Faster AI Reply and Status Text Optimization

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/app`
  - 结果：通过。
  - 覆盖：首轮两个 AI 回复调用会并发开始；普通发送、选择性参与、token usage 和 AI review 服务层回归通过。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

- `rg -n "随后进行互评|AI 正在回复，随后" web internal`
  - 结果：没有匹配项。

### 当前未完成的真实交互验证

- 未调用真实模型供应商测量体感延迟；自动化测试只能证明并发调用开始，不能替代真实网络延迟。
- 当前环境没有浏览器，不能做真实底部状态视觉检查。

### 建议人工验收路径

1. 打开任意至少两个可发言 AI 角色的群聊。
2. 开启或关闭 AI 互评都可以。
3. 发送一条消息。
4. 确认底部状态只显示“AI 正在回复...”，不出现“随后进行互评...”。
5. 用真实模型观察首轮两个回复是否比之前更快出现；实际上限取决于较慢的那个模型供应商响应。

## 2026-05-03 AI Reply Strategy Rollback and Static Cache-Busting Fix

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/app`
  - 结果：通过。
  - 覆盖：三个 AI 角色时首轮恢复为三个回复；短消息跳过互评；长消息为三个首轮回复加一个互评。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：聊天页加载带版本参数的 `/static/chat.js?v=` 和 `/static/theme.js?v=`；AI 互评异步路径仍可追加回复。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

- `rg -n "随后进行互评|AI 正在回复，随后" web internal`
  - 结果：没有匹配项。

### 当前未完成的真实交互验证

- 当前环境没有浏览器，不能直接确认用户浏览器缓存是否已刷新。

### 建议人工验收路径

1. 重启服务后打开聊天页。
2. 查看页面源码或开发者工具 Network，确认 `chat.js` URL 带有 `?v=20260503a`。
3. 发送消息，确认底部不再显示“AI 正在回复，随后进行互评...”。
4. 在三个或更多允许发言角色的群聊发送消息，确认所有允许发言角色都会首轮回复。
5. 确认页面不会在两个 AI 回复后卡住。

## 2026-05-04 AI Review Trigger Simplification

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/app`
  - 结果：通过。
  - 覆盖：AI 互评开启后，短消息也会追加一条互评；长消息同样每轮追加一条互评。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：AI 互评开启后，异步发送普通短消息也能在消息更新接口中看到 `review from`。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 当前环境未调用真实模型 API，因此真实浏览器里的互评内容质量仍需用户验收。

### 建议人工验收路径

1. 打开一个至少两个允许发言 AI 角色的群聊。
2. 开启 AI 互评。
3. 发送一条短消息，例如“测试互评”。
4. 等待首轮 AI 回复完成。
5. 确认之后追加 1 条互评回复。

## 2026-05-04 AI Review Polling Fix

### 自动化验证

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./internal/http`
  - 结果：通过。
  - 覆盖：聊天页加载 `/static/chat.js?v=20260504a`，AI 互评异步路径仍可追加 `review from`。

- `env GOCACHE=/tmp/go-build-ai-chat go test -mod=mod ./...`
  - 结果：通过。

- `env GOCACHE=/tmp/go-build-ai-chat go build -mod=mod -buildvcs=false ./cmd/server`
  - 结果：通过。

### 当前未完成的真实交互验证

- 当前环境没有浏览器，不能验证真实页面是否在互评生成期间持续轮询。

### 建议人工验收路径

1. 重启服务并刷新聊天页。
2. 确认页面加载的脚本是 `/static/chat.js?v=20260504a`。
3. 开启 AI 互评。
4. 发送“测试互评”。
5. 确认三条首轮 AI 回复之后，当前页面继续追加 1 条互评回复。
